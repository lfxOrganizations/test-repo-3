name: Welcome First-Time Contributors
on:
  pull_request:
    types: [opened, reopened, ready_for_review]

permissions:
  pull-requests: write
  contents: read
  issues: write  # Add this

jobs:
  welcome:
    uses: yashgoyal0110/.github/.github/workflows/pull-request-comment.yml@feat/pr-comment-gha
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}  # Pass the token explicitly
    with:
      comment-body: |
        👋 Welcome @${{ github.actor }}!
        
        This looks like your **first PR here** — thank you for contributing 🎉  
        A maintainer will review it soon. Let us know if you need help!
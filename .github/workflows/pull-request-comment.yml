name: On PR Open – First-Time Contributor Comment

on:
  pull_request_target:
    types: [opened]

permissions:
  pull-requests: write
  issues: write

jobs:
  call-first-pr-comment:
    uses: yashgoyal0110/.github/.github/workflows/pull-request-comment.yml@feat/pr-comment-gha
